//using Blazor.Fluxor;
using StoryBlog.Web.Blazor.Shared;

namespace StoryBlog.Web.Blazor.Client.State.Cart
{
    /*public sealed class GetCartStateFailedActionReducer : Reducer<CartState, GetCartStateFailedAction>
    {
        public override CartState Reduce(CartState state, GetCartStateFailedAction action)
        {
            StateMutator.For<CartState>().Mutate(state, action);
            return CartState.Failed(action.CartId, action.ErrorMessage);
        }
    }*/
}